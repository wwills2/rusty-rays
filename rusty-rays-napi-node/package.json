{
  "name": "rusty-rays-napi-node",
  "version": "1.0.0",
  "description": "NAPI bindings for rusty-rays",
  "scripts": {
    "build:napi": "rimraf bindings && napi build -o bindings --release --platform --js index.cjs",
    "build": "rimraf dist && npm run build:napi",
    "test": "npm run build && rimraf test.js && tsc && node test.js"
  },
  "type": "module",
  "engines": {
    "node": ">=22.19"
  },
  "author": "William Wills",
  "devDependencies": {
    "@napi-rs/cli": "^3.1.5",
    "@types/node": "^24.3.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  }
}
